{
  "name": "verification-webhook-server",
  "version": "1.0.0",
  "description": "Simple webhook server to receive verification codes",
  "main": "webhook-server.js",
  "scripts": {
    "start": "node webhook-server.js",
    "dev": "nodemon webhook-server.js",
    "vercel-build": "echo 'Build complete'",
    "vercel-dev": "vercel dev"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}